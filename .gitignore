# dependências e build
node_modules/
.next/
out/
dist/
.vercel/

# ambiente — segredo nunca vai para o git
.env
.env.local
.env*.local

# sistema
.DS_Store
Thumbs.db

# material de marca: arquivos-fonte pesados (~85 MB no total).
# Ficam locais de propósito — inflariam o clone permanentemente.
# Ver CLAUDE.md para o inventário e como abrir cada um.
*.cdr
*.pdf
IMG_*.png

# artefatos de screenshot do Playwright
.playwright-mcp/
nbrasa-*.png

# artefato de build do TypeScript
*.tsbuildinfo

# artefatos de processo: ficam locais, fora do repositorio
docs/superpowers/plans/
docs/superpowers/decisoes/
docs/superpowers/specs/mockup-direcao-visual.html
